import "./style.scss";

const Loader = () => {
  // const [loader, setLoader] = useState();

  return (
    <div className="loader loaderOverlay ">
      <img src="/loader.gif" />
    </div>
  );
};

export default Loader;
